{
  "name": "api-lambda-crud-dynamodb",
  "version": "1.0.0",
  "description": "Running an API Gateway with four Lambdas to do CRUD operations on DynamoDB",
  "scripts": {
    "postinstall": "(cd lambdas && npm install);",
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "test": "jest"
  },
  "author": {
    "name": "Luis Alfonso Preciado <luisapreciado99@gmail.com>"
  },
  "license": "MIT",
  "devDependencies": {
    "@aws-sdk/types": "^3.127.0",
    "@types/jest": "^26.0.10",
    "@types/node": "*",
    "@types/uuid": "^8.3.4",
    "aws-cdk": "*",
    "aws-sdk-client-mock": "^1.0.0",
    "esbuild": "*",
    "jest": "^26.4.2",
    "jest-junit": "^13.0.0",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~4.6.0"
  },
  "dependencies": {
    "@aws-sdk/lib-dynamodb": "^3.154.0",
    "aws-cdk-lib": "2.8.0",
    "aws-sdk": "^2.1199.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.16"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "node_modules/variables/.+\\.(j|t)sx?$": "ts-jest"
    },
    "transformIgnorePatterns": [
      "node_modules/(?!variables/.*)"
    ]
  }
}
